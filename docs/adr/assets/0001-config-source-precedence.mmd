---
title: Config source precedence (ADR 0001) - illustrative example, delete with the ADR
---
graph TB
    subgraph Sources["Layered sources - top overridden by bottom"]
        D["Class defaults\ndeclared on the BaseSettings subclass\ne.g. service_name: str = 'zxre'"]
        F[".env file (if present)\nproject-root .env, checked in for local dev"]
        E["Process environment variables\ne.g. SERVICE_NAME=billing-api\nhighest precedence - always wins in deployment"]
    end

    D -->|overridden by| F
    F -->|overridden by| E
    E --> R["Resolved settings instance\ne.g. HealthConfig()\ntyped + validated at construction"]

    R -.->|"bypassed entirely by\nconstructor kwargs in tests"| T["Unit tests\nHealthConfig(service_name='test')"]

    style T stroke-dasharray: 4 3
